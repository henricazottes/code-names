div(class="item")
  div(class=`content ${me ? 'my-name' : ''}`)
    = ` ${name} `
    if isCaptain
      i(class="fas fa-star" style="color: #fdcb6e")
  if showActions
    div(class="actions"  data-socketid=socketId)
      button(class="userKickUser") ❌